import React from 'react';


const NoDisplay = (props) => {
  return (
    <div className="no-display">Sorry, touch screen and mobile devices are not currently supported : (</div>
  )
}

export default NoDisplay;
